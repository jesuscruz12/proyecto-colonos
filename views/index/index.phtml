<!DOCTYPE html>
<html lang="es">
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <title>COLONOS | Acceso al sistema</title>

    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- Fuente -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@fontsource/source-sans-3@5.0.12/index.css" crossorigin="anonymous">

    <!-- OverlayScrollbars -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/overlayscrollbars@2.3.0/styles/overlaysscrollbars.min.css" crossorigin="anonymous">

    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.min.css" crossorigin="anonymous">

    <!-- AdminLTE -->
    <link href="<?php echo $_layoutParams['ruta_css']; ?>adminlte.css?<?php echo time(); ?>" rel="stylesheet">

    <style>
        :root {
            --taktik-primary: #0f172a;
            --taktik-primary-soft: #1e293b;
            --taktik-accent: #fe8304;
            --taktik-bg: #f5f7fa;
            --taktik-text-main: #111827;
            --taktik-text-soft: #6b7280;
        }

        body.login-page {
            min-height: 100vh;
            margin: 0;
            font-family: "Source Sans 3", system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
            background: radial-gradient(circle at top left, #e8edf7 0, #f5f7fa 55%, #eef2ff 100%);
            color: var(--taktik-text-main);
        }

        .login-wrapper {
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 1.5rem;
        }

        .login-box {
            width: 100%;
            max-width: 1040px;
        }

        .login-card {
            border-radius: 1.5rem;
            overflow: hidden;
            background-color: #ffffff;
            box-shadow: 0 18px 45px rgba(15, 23, 42, 0.18);
            border: 1px solid rgba(209, 213, 219, 0.9);
        }

        /* Panel izquierdo */
        .login-hero {
            background-color: var(--taktik-primary);
            color: #fff;
        }

        .login-hero-content {
            height: 100%;
        }

        .login-hero-logo-main {
            background-color: #ffffff;
            border-radius: 999px;
            padding: 0.45rem 1rem;
            font-weight: 800;
            color: var(--taktik-primary);
            letter-spacing: 0.12em;
        }

        .badge-soft {
            border-radius: 999px;
            padding: 0.25rem 0.7rem;
            font-size: 0.75rem;
            background: rgba(255,255,255,0.15);
            border: 1px solid rgba(255,255,255,0.35);
        }

        .login-hero-title {
            font-size: 1.7rem;
            font-weight: 800;
            margin-top: 1.4rem;
        }

        .login-hero-subtitle {
            font-size: 1rem;
            opacity: 0.95;
        }

        .login-hero-option {
            border-radius: 0.9rem;
            padding: 0.8rem 1rem;
            background: rgba(255,255,255,0.08);
            border: 1px solid rgba(255,255,255,0.25);
            display: flex;
            gap: 0.75rem;
            margin-bottom: 0.6rem;
        }

        .login-hero-option span.value {
            font-weight: 700;
        }

        /* Panel derecho */
        .login-form-col {
            background-color: #ffffff;
            display: flex;
        }

        .login-form-inner {
            width: 100%;
            padding: 3rem;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .login-title {
            font-weight: 800;
            font-size: 1.5rem;
        }

        .login-subtitle {
            font-size: 0.95rem;
            color: var(--taktik-text-soft);
        }

        .btn-primary {
            background-color: var(--taktik-accent);
            border-color: #dc6f00;
        }

        .btn-primary:hover {
            background-color: #dc6f00;
        }

        .btn-login {
            border-radius: 999px;
            font-weight: 600;
            padding: 0.8rem 1.25rem;
        }

        @media (max-width: 768px) {
            .login-hero {
                display: none;
            }
            .login-form-inner {
                padding: 2rem 1.5rem;
            }
        }
    </style>
</head>

<body class="login-page">

<div class="login-wrapper">
    <div class="login-box">
        <div class="card login-card">
            <div class="row g-0">

                <!-- HERO -->
                <div class="col-md-6 login-hero">
                    <div class="login-hero-content d-flex flex-column justify-content-between p-5">

                        <div>
                            <div class="login-hero-logo-main mb-3">
                                COLONOS
                            </div>

                            <span class="badge-soft mb-3 d-inline-block">
                                Sistema MES / ERP de Producción
                            </span>

                            <h2 class="login-hero-title">
                                Planeación y control operativo
                            </h2>

                            <p class="login-hero-subtitle mb-4">
                                Plataforma integral para la gestión de órdenes de trabajo,
                                rutas de producción y seguimiento operativo en planta.
                            </p>

                            <div class="login-hero-option">
                                <i class="bi bi-diagram-3"></i>
                                <div>
                                    <span class="value">Órdenes de trabajo</span>
                                    <small>Control total desde la planeación hasta la ejecución.</small>
                                </div>
                            </div>

                            <div class="login-hero-option">
                                <i class="bi bi-gear-wide-connected"></i>
                                <div>
                                    <span class="value">Rutas y procesos</span>
                                    <small>Definición clara de operaciones y secuencias.</small>
                                </div>
                            </div>

                            <div class="login-hero-option">
                                <i class="bi bi-bar-chart-line"></i>
                                <div>
                                    <span class="value">Seguimiento y métricas</span>
                                    <small>Visibilidad en tiempo real del avance productivo.</small>
                                </div>
                            </div>
                        </div>

                        <small class="opacity-75">
                            <i class="bi bi-lock-fill me-1"></i> Acceso seguro · Uso interno
                        </small>

                    </div>
                </div>

                <!-- LOGIN -->
                <div class="col-12 col-md-6 login-form-col">
                    <div class="login-form-inner">

                        <h1 class="login-title mb-1">Acceso al sistema</h1>
                        <p class="login-subtitle mb-3">
                            Ingrese sus credenciales para continuar.
                        </p>

                        <?php if (isset($this->_error)): ?>
                            <div class="alert alert-warning">
                                <i class="bi bi-exclamation-triangle-fill me-2"></i>
                                <?php echo $this->_error; ?>
                            </div>
                        <?php endif; ?>

                        <form action="" method="post" autocomplete="off">
                            <input type="hidden" name="enviar" value="1" />

                            <div class="mb-3">
                                <div class="form-floating">
                                    <input id="usuario" name="usuario" type="text"
                                           class="form-control" placeholder="Usuario" required autofocus>
                                    <label for="usuario">Usuario</label>
                                </div>
                            </div>

                            <div class="mb-3">
                                <div class="form-floating">
                                    <input id="password" name="password" type="password"
                                           class="form-control" placeholder="Contraseña" required>
                                    <label for="password">Contraseña</label>
                                </div>
                            </div>

                            <div class="d-grid">
                                <button type="submit" class="btn btn-primary btn-login">
                                    <i class="bi bi-box-arrow-in-right me-1"></i> Ingresar
                                </button>
                            </div>
                        </form>

                        <p class="text-center small text-muted mt-3">
                            Acceso exclusivo para personal autorizado.
                        </p>

                    </div>
                </div>

            </div>
        </div>
    </div>
</div>

</body>
</html>
